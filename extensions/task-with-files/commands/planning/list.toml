prompt = """
List all planning tasks at a glance.

First, run the list script:
!{node ${extensionPath}/commands/scripts/list-tasks.js}

This will display:
1. All tasks in the working directory
2. Current active task (marked as ACTIVE)
3. Task creation date
4. Goal statement from task_plan.md
5. Full path to task directory

The output helps you:
- Track all your planning sessions
- Find old tasks to resume
- See which task is currently active

Note: Tasks are stored in ${workspacePath}/.agent_working_dir/
"""
